[project]
name = "paperasse-lux-evals"
version = "0.1.0"
description = "Suite d'évaluation pour les skills Paperasse Lux"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.40.0",
    "pyyaml>=6.0",
    "jinja2>=3.1",
    "rich>=13.7",
]

[tool.uv]
package = false
